{
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html html css js assets dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@dataconnect/generated": "file:src/dataconnect-generated",
    "firebase": "^12.2.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}